/*
    将数组的全部元素赋值为(0.0)并显示
*/

#include <stdio.h>

int main(void) {
    int i;
    double vd[5];      /*包含5个元素的数组*/

    for (i = 0; i < 5; i++)
        vc[i] = 0.0;

    for (i = 0; i < 5; i++)
        printf("vc[%d] =  %.1f\n", i, vc[i]);

    return (0);
}
